// Re-exported from @nestjs/throttler so the rest of the app imports
// decorators consistently from @common/decorators/* like everything else
export { SkipThrottle, Throttle } from '@nestjs/throttler';
